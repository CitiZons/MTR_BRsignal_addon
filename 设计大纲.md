# MTR_BRsignal_addon 设计大纲（现状 2026-08-20）

> 定位：Minecraft 1.20.1 / Forge（47.3.0+）/ MTR 4.0.3 Forge 版补充 mod（不改 MTR 原文件，用 Mixin 覆盖信号判定）。
> 命名空间 `mtr_brsignal_addon`，网络协议版本 4。

## 1. 目标

- 最终由服务端根据完整 Vehicle Path 执行闭塞、调度、授权和 Movement Gate。
- 客户端只显示服务端下发的 Aspect、Authorization 和调度信息。
- 当前代码仍主要是显示层实验，不能视为最终执行层。

### 1.1 已有显示功能

- 英式闭塞风格信号显示：绿 → 双黄 → 单黄 → 红；内部 Aspect 数值为 `0=绿, 1=红, 2=黄, 3=双黄`。
- 进路表示器（色灯式 / LED 式）：绑定信号机**非红灯**且存在有效进路时显示；红灯立即熄灭。
- 信号机 ↔ 轨道节点手动绑定（无绑定时回退 MTR 自动寻找）。
- 信号机命名；工具悬停说明；信号机调试面板。

## 2. 信号闭塞逻辑（核心）

### 2.1 闭塞块与占用
- 闭塞块 = 相邻 SignalFace 绑定节点之间的一组 Rail，由服务端按 SignalFace 边界切分。
- `occupied` 表示实际车辆占用；长编组以车尾离开 Section 为释放条件。
- `reserved` 表示 Dispatcher 已批准准备使用；`locked` 表示进路锁闭。三者独立维护。
- 所有闭塞事实由服务端 SectionState 提供；当前客户端读取仅为过渡显示逻辑。

### 2.2 灯序
- 无有效 Authorization、当前 Section 不安全或拓扑失效 → 红；
- 下一同向控制信号红 → 单黄；
- 下一同向控制信号黄 → 双黄；
- 下一同向控制信号为双黄/绿，或已授权进路到达明确 Terminal → 绿。

显示顺序为：`绿 → 双黄 → 单黄 → 红`。

### 2.3 服务端调度链

```text
Vehicle immutablePath
  -> triggerDistance = min(下一运营停车点, 前方约4个同向信号控制边界)
  -> 一列车一个基于 immutablePath 的完整 RouteRequest
  -> SectionCheck
  -> Dispatcher（FCFS 或人工选择）
  -> Authorization（当前实际获批并锁闭的前缀，可动态扩展/释放）
  -> 多个 Signal Aspect
  -> Movement Gate
```

WAITING/DENIED 在 SectionState、拓扑或车辆状态变化时立即 dirty 并重检，周期任务只作兜底。拓扑失效时撤销授权、停车、重新生成 Path 和 Request，默认不自动换路。

### 2.4 人工驾驶 Override

- `Dispatcher Override` 只改变调度选择，不绕过安全检查。
- `Manual Driving Override` 仅适用于确认处于人工驾驶的车辆，可越过红灯并进入 occupied Section。
- Override 只影响该车辆；Section 仍记录真实 `occupied`，自动车辆仍必须把该 Section 视为不可进入。
- 服务端记录 Override 和异常占用，客户端不能单独授予权限。

### 2.5 未实现（规划中）
- 进路读取“只读到下一停站”的截断（当前读到线路终点/下一信号机）。
- 父-子信号机手动绑定（接口 `ParentChildSignalBinding` 已留，未实现；当前按列车进路自动识别）。
- 执行层（让列车按闭塞真实停车/减速）。

## 3. 工具与绑定

### 信号机调试工具
- 右键信号机：调试面板（命名/灯状态/作用节点/进路编辑/指示器解绑）。
- Shift+右键信号机 → Shift+右键轨道节点：节点绑定。
- Shift+右键进路指示器 → Shift+右键信号机：指示器绑定（顺序不可反）。
- 右键进路指示器：信息面板（显示绑定信号机命名或坐标、可用显示、解绑）。
- 左键轨道节点不破坏方块（仅拦截）。

### 进路工具
- 右键信号机选中 → Shift+右键节点：绑定进路并弹输入框（`route=1|2|4|5` 或 `path=0..20/A-Z/UF,US,DF,DS`；`UP/DW` 已弃用）。
- 手持时显示绿色连线 + 靠近信号机的标签；乘车上显示下一信号机进路。

### 命名
- ≤40 字符，字母/数字/下划线/连字符，不能以 `_` 或 `-` 开头；空值清除。

## 4. 数据与持久化

- 服务端 `RouteBindingsSavedData`：进路绑定、节点绑定、指示器绑定、信号机命名；与方块实体 NBT 双写。
- 登录/变更时 `SyncRouteBindingsPacket` 全量同步到客户端；渲染与 UI 优先读客户端镜像。
- 网络包：SetRouteBinding / RemoveRouteBinding / SetNodeBinding / ToggleNodeBindingDirection / BindIndicator / UnbindIndicator / SetSignalName / SyncRouteBindings（协议版本 4）。

## 5. 渲染

### 色灯式（运行时 bbmodel）
- `ColorLightModel` 运行时解析 `indicator_1_NULL.bbmodel`（逐面 UV、多贴图、元素旋转），`ColorLightIndicatorRenderer` 用 MTR `MainRenderer` 按贴图分组绘制。
- route 图层按“非红灯且有 route 绑定”显隐；blockstate 指向空模型。

### LED 式
- `led_indicator.json` 构建期生成（含 22.5/45/67.5 变体）；`LedIndicatorRenderer` 画 path 贴图四边形。
- 显示条件同上（非红灯且有 path 绑定，`path=NULL` 不显示）。

### 旋转
- `is_22_5`/`is_45` 属性 + 16 变体；色灯式渲染器 `-(angle)`、LED `-(angle+180)`。

### 世界连线
- 调试工具：红色透明粗线连接信号机↔作用节点；进路工具：绿色透明粗线 + 绑定内容标签；手持工具时轨道节点可视化。

## 6. 工程结构

```
org.mtrbr
├─ api          ParentChildSignalBinding（预留接口）
├─ block        两个指示器方块 + 方块实体（NBT 持久化指示器绑定）
├─ client       ClientHooks / SignalCache / CenterToast / TooltipRenderer
├─ data         SavedData / 客户端镜像 / 内容校验与贴图映射
├─ event        LeftClickHandler（工具不破坏方块）
├─ item         信号机调试工具 / 进路工具（Shift 悬停提示）
├─ logic        SignalLogic（闭塞链、区段占用、角度）
├─ mixin        BlockSignalBlockEntityMixin（覆盖 MTR getActualAspect）
├─ network      8 个数据包
├─ render       两个指示器渲染器 / 覆盖层 / 车上 HUD
└─ screen       信号机调试 / 进路输入 / 指示器信息
```

## 7. 已知问题与待决策

- LED path 显示方向：数字/双字母组合曾报倒置，贴图已回退原始方向；待确认当前渲染方向是否符合预期。
- 色灯式运行时渲染的朝向/尺寸仍在验证（正面补黑后应可见）。
- 岔区羊毛信号作为区段端点的取舍：当前**全部参与**；如需只让本 mod 绑定过的信号机参与，需加过滤。
- 执行层可行性（服务端 Simulator 或核心 Mixin）风险最高，独立评估。

# MTR_BRsignal_addon 内容说明

> 当前实现与资源约定：2026-09-08（0.1.2 更新版，网络协议 9）<br>
> Minecraft 1.20.1 / Forge 47.4.18 / MTR Forge 4.0.3  
> 命名空间：`mtr_brsignal_addon`  
> 许可证：[MIT License](LICENSE)

本文档合并原 `设计大纲.md`、`迁移计划.md` 和 `模型.md`，并收录 README 中适合长期查阅的详细内容。README 只保留快速入门和常用操作；本文记录项目的定位、当前架构、资源约定、工程边界和后续计划。

## 1. 项目定位

MTR_BRsignal_addon 是 Minecraft Transit Railway（MTR）的英式铁路信号扩展 mod，用于 Minecraft 1.20.1 Forge 环境。项目不修改 MTR 源码或原始 JAR，只在 addon 内通过 Mixin、服务端状态管理和客户端显示层接入 MTR。

项目的核心目标是：由服务端根据列车完整 `immutablePath` 维护闭塞、进路请求、调度、授权、资源生命周期和 Movement Gate；客户端只显示服务端同步的信号状态、授权状态、调度信息和诊断可视化。

当前已经形成以下主链路：

```text
Vehicle immutablePath
  -> RouteRequest（完整进路请求）
  -> RouteProjection（当前 occurrence、边界和资源集合）
  -> SectionCheck
  -> Dispatcher（FCFS 或人工选择）
  -> Authorization（当前可安全开放的前缀）
  -> Signal Aspect
  -> Movement Gate
```

Request 表示一列车对完整物理进路的申请；Authorization 是同一 Request 当前实际获批并锁闭的安全前缀，二者不能通过缩短 Request 来代替。

## 2. 已实现功能

### 2.1 信号与闭塞

- 英式灯序：绿、双黄、单黄、红；内部 aspect 数值为 `0=绿, 1=红, 2=黄, 3=双黄`。
- 服务端维护 `SignalFace`、`Section`、canonical `Block`、`JunctionMovement`、`RouteRequest`、`Authorization` 和 `Movement Gate`。
- 没有有效 Authorization、当前区段不安全或拓扑无效时，信号默认显示红灯。
- 下一同向控制信号为红时显示单黄；为黄时显示双黄；前方安全且处于有效授权前缀时显示绿灯。
- 闭塞区间由相邻 SignalFace 绑定的轨道节点之间的 Rail 集合构成，不以单个 `rail.getHexId()` 作为互斥单位。
- `occupied`、`reserved`、`locked` 独立维护；Section 负责实际占用和逐段释放，闭塞区间负责互斥、授权前缀和停车边界。
- 长编组以车尾离开对应资源作为释放依据。Section 释放不等于 Request 释放；Request 只有完整进路完成后才进入 `PASSED -> RELEASED`。
- 运行中授权保存建立时取得的 Block occurrence、Section ID、JunctionMovement ID 和路径上下文；释放阶段使用这份资源快照，不重新切片 traversal。
- `DENIED`、`REVOKED`、`CANCELED` Request 本身不构成资源持有理由；stale 清理只保留 Authorization 或仍与车辆物理范围相交的 pending occurrence。
- 拓扑失效时撤销授权、释放可释放资源并停车，默认不自动换路。
- 折返边界由完整计划路径中的方向反转识别。接近折返站最后约 4 格时交还 MTR 原生终到计算，以触发原生开门、终到和反向行为。

### 2.2 指示器与复示信号机

- 支持色灯式进路指示器和 LED 式进路显示器。
- 支持复示信号机，绑定主信号机后根据主信号状态显示 `on`、`off` 或 `off_limiting`。
- 进路指示器只有在绑定存在、主信号机不是红灯且存在有效进路/授权时显示；红灯或无有效授权时熄灭。
- 支持信号机到轨道节点的手动绑定；没有手动绑定时回退到 MTR 自动寻找的节点。
- 支持信号机命名、节点绑定方向切换、指示器解绑和支撑/下挂安装方式切换。
- 复示信号机和进路指示器拆除时，服务端会清理其绑定数据并向客户端广播新的快照，诊断蓝线随之消失。

### 2.3 工具与游戏内界面

信号机调试工具：

- 右键信号机：打开调试面板，可命名、查看灯状态、查看作用节点、编辑进路和查看/解绑指示器。
- Shift+右键信号机，再 Shift+右键轨道节点：手动绑定作用节点。
- Shift+右键进路指示器或复示信号机，再 Shift+右键信号机：绑定到主信号机。
- 左键轨道节点只拦截工具行为，不破坏节点方块。

进路工具：

- 右键信号机选中目标，再 Shift+右键轨道节点绑定进路并输入内容。
- 色灯式内容格式为 `route=1` 至 `route=6`。
- LED 内容格式为 `path=0..20/A-Z/UF,US,DF,DS` 等支持的短码。
- 手持时显示绿色进路连线和内容标签；乘车时显示下一信号机进路 HUD。

调度工具和调度台：

- 右键空气、调度台方块或使用调度工具打开调度面板。
- 面板显示车辆、Request、Authorization、Section 占用、预留和锁闭状态。
- Approve、Revoke、Override 和优先级操作只改变调度选择，仍必须通过服务端安全检查。
- 底部五个调度按钮与新增三个 token 按钮分别按窗口宽度居中；列表绘制、滚动和点击区域为两排按钮预留空间。
- “生成 Token”通过服务端发放后自动打开默认浏览器 WebUI，失败原因只发送给本人；自动打开响应必须对应本次按钮请求。
- “Token 列表”打开个人聊天框并执行 `/mtrbr web_token list`。“销毁 Token”列出带可点击销毁项的个人 token，点击按具体 token 校验归属并撤销，不受列表重新编号影响。

命名约束：最多 40 个字符；允许字母、数字、下划线和连字符；不能以下划线或连字符开头；空值清除名称。

### 2.4 Web 调度界面

启用 MTR Web server 后，OP 可以通过 `/mtrbr web_token generate` 获取带权限的 Web 地址。Web 界面提供：

- 实时 Section、站台、信号机和车辆短码地图。
- Section 的 occupied、reserved、locked 状态。
- Request 抽屉，显示 `Code`、`State`、`Route`、`Next` 和 `Dest`。
- 点击请求条目或地图车辆短码后，将地图居中并高亮相关请求区段；再次点击可取消选中。
- 信号机命名和浏览器内图标位置调整。图标位置只影响 Web 显示，不改变游戏绑定。
- Depot LINE 路径预览、候选节点预检、方向约束检查、保存和详细无效路径诊断。
- OP 权限状态和服务器拒绝原因。

Web token 规则：

- 每个 token 绑定一台浏览器设备；同一 token 被其他设备使用时永久标记为 `LEAKED`。
- OP 离线后，该 OP 的有效 token 永久标记为 `PLAYER_OFFLINE`。
- 每个 OP 最多持有 5 个 token，有效期 8 小时；到期 token 在下次整理时移出名额，离线或泄露的 token 仍需手动撤销。
- 无 token 或失效 token 只能读取，不能调度、改名或执行其他写操作。
- 专用服务器必须配置 `web_public_host`，addon 不猜测公网地址。
- `/mtrbr web_token list_all` 按玩家显示当前有效 token，只回复发起命令的 OP，不广播给其他玩家或其他 OP；已到期、撤销、离线、泄露或持有人已无 OP 权限的 token 不列入有效项。
- `/mtrbr web_token disable <player>` 和 `enable <player>` 控制指定玩家生成新 token 的能力；仍沿用 OP 权限要求，禁用不撤销已有 token。按玩家 UUID 保存到主世界的 `mtr_brsignal_addon_web_token_permissions.dat`，跨维度生效并在重启后保留；token 本身仅保存在内存中，重启后失效。
- 个人列表、生成地址和撤销反馈均只发送给本人。禁止生成的玩家仍可查看和撤销自己已有的 token。
- Web 的车辆删除流程必须依次通过 `QUARANTINE`、`DELETE`、`CONFIRM`，并受与命令相同的受限清理规则保护。
- 日志记录路径预检、调度操作、信号命名、操作者和失败上下文，不记录 token 或 device ID。

## 3. 服务端架构与安全边界

### 3.1 不可违反的原则

1. 只修改本 addon，不修改 MTR 源码或原始 JAR。
2. 服务端 `SectionState` 是闭塞和占用事实的唯一权威；客户端不得参与安全、FCFS、Authorization 或停车判断。
3. Request 基于完整 `immutablePath` 创建；不能通过缩短 Request 来控制绿灯范围。
4. Authorization 是 Request 的安全前缀，当前上限为 `min(下一运营停车点, 下一折返边界, 后五个同向信号距离, 当前调车进路的第一段闭塞终点)`；无调车进路时最后一项不限制。
5. 自动调度使用 FCFS；人工调度只能覆盖选择顺序，不能绕过 SectionCheck、reserve、lock 或 Movement Gate。
6. 没有有效授权时信号为红灯；Movement Gate 才是实际阻止车辆运行的执行层。
7. Section 逐段释放，Request 在完整进路结束后释放；车辆仍物理占用的资源不可提前清理。
8. 拓扑失效必须撤销授权并停车，不自动换路。

### 3.2 持久化与同步

`RouteBindingsSavedData` 持久化以下内容：

- 信号机到进路节点的绑定。
- 信号机到作用节点的绑定及方向。
- 进路指示器、LED 显示器和复示信号机到主信号机的绑定。
- 信号机名称。

指示器绑定同时写入方块实体 NBT 和 SavedData，防止区块保存时序导致丢失。登录和数据变化时，`SyncRouteBindingsPacket` 将完整镜像发送到客户端；客户端渲染和界面只读取这个镜像及服务端状态缓存。

### 3.3 诊断可视化

- 调试工具：红色透明粗线连接信号机和作用节点。
- 调试工具：蓝色透明粗线连接进路指示器/复示信号机和主信号机。
- 进路工具：绿色透明粗线连接信号机和进路节点，并显示绑定内容标签。
- 手持相关工具时，附近轨道节点显示可视化标记。

## 4. 模型、贴图与渲染约定

### 4.1 渲染总览

| 方块 | 内容 | 模型来源 | 渲染方式 |
| --- | --- | --- | --- |
| `indicator_1` 及其变体 | `route=1..6`，按具体型号支持的灯位显示 | null 外壳模型 + route JSON | `ColorLightIndicatorRenderer` 按元素边界绘制灯块 |
| `led_indicator` | `path=...` | `led_indicator.bbmodel` 生成 JSON | `LedIndicatorRenderer` 绘制屏幕贴图四边形 |
| `banner_repeating_signal` | 主信号 aspect | 静态外形 JSON + 动态状态 JSON | `RepeatingSignalRenderer` 运行时绘制状态面 |
| `signal_bracket` | 支架 | 静态 JSON | blockstate 根据朝向选择变体 |
| PSR / AWI 及箭头牌 | 装饰性限速/车型文字、方向箭头 | `models/speed_sign_artwork.json` + 平滑贴图/字形图集 | `SpeedSignRenderer` 动态绘制，不参与信号逻辑 |

色灯式 route 模型首次解析后会写入客户端 `config/mtr_brsignal_addon/route-light-cache.json`。修改 route 模型后必须删除该缓存再启动游戏。LED 的 `.bbmodel` 修改后需要重新生成 JSON。

### 4.2 贴图约定

资源根目录为 `src/main/resources/assets/mtr_brsignal_addon/`。

- `textures/block/indicator/`：色灯式面板、背板和 route 图层。
- `textures/block/repeating_signal/`：复示信号机的 `on`、`off`、`off_limiting` 状态贴图。
- `textures/block/path/`：62 张 LED 数字、字母、方向和组合短码贴图，统一为 21×21；已在 Blockbench 中逐张细调，保留黑底白色像素风格、对称性和相近符号的区别。编辑时逐张修改源图，不用批量缩放覆盖手工笔画。`python tools/preview_path_textures.py` 只读取 `path_*.png`，输出 `build/previews/path-textures.png`，预览不放入游戏资源目录。构建检查验证贴图尺寸。
- `textures/block/white.png`：全亮灯块、工具线框和通用白色材质。
- `textures/block/grey.png`、`black.png`：机壳、立柱和未贴图面填充。

色灯式 `indicator_1_NULL.bbmodel` 的 UV 空间为 32×32，与 PNG 实际尺寸无关；运行时解析器会按 32 空间归一化。正面面板使用 `indicator_1`，背面使用 `indicator_1_back`。

### 4.3 进路内容映射

- `route=X`：`X` 为 `1` 至 `6`，对应具体型号 route 模型中的灯块。
- `path=Y`：支持 `0..20`、大写字母、`UF`、`US`、`DF`、`DS` 等组合，以及资源目录中定义的小写箭头组合。
- `route=NULL` 或 `path=NULL`：不显示动态内容。
- 动态内容只有在服务端确认绑定有效、主信号非红且存在有效授权时显示。

### 4.4 旋转和安装

- 相关方块使用 `is_22_5`、`is_45` 属性组合出普通、22.5°、45° 和 67.5° 变体。
- blockstate 同时覆盖四个水平朝向和支撑/下挂状态。
- 复示信号机、LED 和色灯式指示器的碰撞箱由模型边界生成，碰撞箱应与模型位置和尺寸一致。
- 修改支撑/下挂方式只改变安装状态，不改变绑定关系或进路方向。

资源修改流程：

1. 修改色灯外壳或 route 模型。
2. 如 route 灯块几何发生变化，删除 `route-light-cache.json`。
3. 修改 LED `.bbmodel` 后重新生成对应 JSON 旋转变体。
4. 替换 PNG 时保持路径、命名、透明度和 UV 约定。
5. 执行资源检查和完整 build。

## 5. 工程结构

```text
org.mtrbr
├─ api       ParentChildSignalBinding（父子信号机预留接口）
├─ block     指示器、复示信号机、支架和方块实体
├─ client    客户端缓存、事件入口、提示和 HUD
├─ data      SavedData、客户端镜像、内容校验和贴图映射
├─ event     工具交互事件
├─ item      信号机调试工具、进路工具、调度工具
├─ logic     信号显示、复示状态和纯逻辑工具
├─ mixin     MTR 信号、车辆和路径接入
├─ network   C2S/S2C 网络包
├─ render    指示器、复示信号机、远距渲染和世界覆盖层
├─ screen    信号调试、进路输入、指示器和调度界面
├─ server    SignalFace、Section、Block、Request、Authorization 和 Gate
└─ web       Web API、拓扑快照、Depot 路径编辑和会话管理
```

## 6. 常用命令与运维

```text
/mtrbr requests                                    # 列出进路请求、授权和锁定状态
/mtrbr approve <vehicle_code>                      # 手动批准当前进路请求
/mtrbr revoke_pending <vehicle_code>               # 撤销尚未生效的待处理授权
/mtrbr manual_override <vehicle_code> <true|false> # 开关人工驾驶覆盖
/mtrbr priority <vehicle_code> <value>             # 设置调度优先级
/mtrbr quarantine confirm_removal <vehicle_code>   # 确认清理 quarantine 车辆
/mtrbr audit                                       # 输出进路、闭塞和资源审计
/mtrbr protection regenerate                       # 按当前拓扑重建保护定义
/mtrbr web_token generate                          # 生成 Web 调度地址
/mtrbr web_token list                              # 列出自己的 token
/mtrbr web_token revocation                        # 列出可点击销毁的个人 token
/mtrbr web_token revocation <number>               # 撤销指定 token
/mtrbr web_token list_all                          # OP 私下查看所有有效 token，按玩家分组
/mtrbr web_token disable <player>                  # 禁止该玩家生成新 token（持久化）
/mtrbr web_token enable <player>                   # 恢复该玩家生成新 token
```

首次部署或调整信号拓扑后，建议执行 `/mtrbr protection regenerate`。运行日志可重点检索：

```text
MTRBR-ROUTE-PROJECTION
MTRBR-AUTH-*
MTRBR-RESOURCE-RELEASE
MTR_TURNBACK_*
MTRBR-DEPARTURE-GUARD
```

## 7. 构建、检查与验证

环境要求：JDK 17、Gradle wrapper、MTR Forge 4.0.3 依赖 JAR 位于 `libs/`。

```powershell
.\gradlew.bat compileJava --no-daemon
.\gradlew.bat build --no-daemon
powershell -NoProfile -ExecutionPolicy Bypass -File tools\check_regressions.ps1
python tools/check_triple_indicators.py
python tools/check_six_route_indicators.py
python tools/check_signal_mounts.py
python tools/check_speed_signs.py
```

`build` 会执行 Java 编译、资源处理和回归任务，包括 LINE 路径、进路指示器、复示信号机模型、折返路径、调度生命周期、限速牌文字/安装位置以及 token 权限/生命周期检查。构建输出位于 `build/libs/`。Python 资源检查依赖 Pillow，调车预览另外依赖 NumPy。构建成功不代表已在游戏内完成画面和浏览器联动验证。

0.1.2 更新版使用网络协议 `9`。客户端与服务器应同时安装本次构建；不要混用同版本号的早期 JAR。发布说明见 [CHANGELOG.md](CHANGELOG.md)。本地部署脚本 `build_deploy_alpha4.ps1` 和运行日志不纳入版本跟踪。

验证重点：

- 单列车申请进路，确认授权前缀、信号灯序和 Movement Gate 一致。
- 两列车接近同一闭塞区间，确认只有一列车可进入。
- 车头进入、车尾离开 Section，确认 Section 独立释放而 Request 生命周期仍正确。
- 修改轨道拓扑，确认授权撤销、车辆停车、Request 重建且不自动换路。
- 拆除复示信号机或进路指示器，确认服务端绑定、客户端镜像和蓝色连线同时清除。
- 修改模型后删除客户端几何缓存，再检查模型、碰撞箱和四种旋转状态。

## 8. 迁移历史与后续计划

### 8.1 已完成的迁移方向

项目从“客户端推测闭塞 + 服务端逐步接管”的过渡架构，迁移到服务端权威链路。主要完成方向包括：

- 统一 SectionState、SignalFace、canonical Block occurrence 和资源释放语义。
- 将完整 `immutablePath`、RouteRequest 和 Authorization 前缀分离。
- 以车尾物理范围和建立时资源快照为释放依据。
- 将 Movement Gate 接入车辆运行，处理授权边界、红灯停车和折返前的 MTR 原生交接。
- 将信号、调度和指示器显示改为读取服务端同步状态。
- 加入 LINE 路径编辑、Web 调度、车辆短码、审计日志和回归测试。

### 8.2 保留的护栏与待办

以下事项属于后续改进，不应通过放宽安全检查来解决：

- 继续验证 `SignalBlockSavedData.rebuild` 在岔区、多节点和同一节点多 face 场景下的边界选择。
- 评估 `railBlockedDistance` 的禁用范围，确保未受控车辆不会因 Movement Gate 边界缺失而获得无限运行距离。
- 保持无 Request、前方有信号或边界为 NaN 时的保守停车行为。
- 色灯式和 LED 式指示器均读取服务端授权显示缓存，远距渲染继续使用统一授权数据。
- 调度面板已支持搜索、状态筛选、表头排序、列表滚动和长字段悬浮查看。
- SectionState 快照采用状态变化立即发布、每 20 tick 兜底；信号计算每 10 tick 运行一次，显示变化时同步、每 20 tick 全量兜底。
- VehicleCode 使用 4 位 base35 顺序码，字符集排除大写 `O`；底层安全逻辑仍使用原始 `vehicleId`。

任何后续实现都必须满足：服务端权威、完整路径身份不变、授权前缀可动态扩展、实际占用资源不可提前释放、拓扑失效停车且不自动换路。

## 调车信号（Position Light Signal）

提供 `position_light_signal`（红灯型，中文名“调车信号”）和 `yellow_position_light_signal`（黄灯型）。采用 current 三灯布局：关闭时落地式下方两灯水平双红／双黄，开放时上方及右下方两灯组成 45° 双白；贴杆式未开放时全部熄灭，遵从主信号。没有 pre-1996 的红白／黄白混合关闭显示，也没有文字铭牌、箭头和永久停止型 Limit of Shunt。

- 用调试工具 Shift+右键调车信号，再 Shift+右键主信号，建立必需绑定。蓝色连线、解绑和拆除清理沿用显示设备的工作流。
- 用进路工具选中主信号，再 Shift+右键目标节点，输入 `shunt=名称`，例如 `shunt=yard_1`。名称允许 1–32 个英文字母、数字、下划线或连字符，保存时转为小写。`route=` 和 `path=` 保持原有用途。
- 同一目标节点可以输入 `path=1 || shunt=yard_1`，也可以同时输入 `route=2 || path=1 || shunt=yard_1`；每种前缀最多一项，保存时按 route、path、shunt 排序。组合中的 shunt 控制调车授权，route/path 驱动对应进路指示器，完整组合一并保存和同步。输入界面将 route/path 可用选项列在第一行，shunt 选项列在第二行，再次打开可编辑已有组合。
- 以列车当前方向的路径匹配目标节点。目标节点可位于第一段闭塞以外；它用于选择调车进路，不把开放范围延伸到该节点。多个绑定同时位于路径上时，选择最近的前方目标；反向行程的节点不用于当前行程。
- 主信号在调车进路中保持红灯，并参与前序信号的黄灯／双黄灯递推。调车双白仅在该方向第一段闭塞完整获准、无其他车辆占用及资源冲突时开放；本车已有有效授权时可以不停车通过这个主信号。
- 没有绑定调车信号时不启用调车模式；仅保留 `shunt=` 进路文本不会把主信号永久压红。拆除最后一个绑定的调车信号后恢复普通授权与灯色判断；已加载区块中确认不存在的残留设备绑定也会清除，未加载区块不会被当作已拆除。
- `VehicleMixin` 在有效授权期间跳过 MTR `stoppingCooldown` 对旧停车点的复用，重新取得原生站停/终到位置，再由 `MovementGate` 限制到当前授权终点；不额外要求在调车入口停车。
- 越过调车信号代表的节点后可立即申请下一段；在越过无调车设备或使用普通主信号放行的节点前，只能再申请一个前方闭塞。连续调车信号逐个越过、逐段申请；越过普通主信号节点后恢复普通授权窗口。原有车尾占用及资源释放规则继续适用。
- 下一信号提前取得授权并发布属于本车、本次授权的开放显示后，可不停车连续通过。若未开放，仍在该节点拦停；附近的微小位置误差不会提前隐藏该信号。下一信号控制调车进路时，等待其调车双白开放显示。
- 调试工具右键调车信号打开设置，切换“落地／贴杆”。落地支脚接触方块底面；贴杆式包含与原生 MTR `signal_pole` 相同的中心 4×4 柱段，可与上下方信号柱接续。灯箱和抱箍随 16 方位旋转，柱段保持原生轴对齐。

模型位于 `src/main/resources/assets/mtr_brsignal_addon/models/block/position_light_*.json`，均为 Minecraft Java 1.20.1 可用且可在 Blockbench 中打开的方块 JSON。基础模型是 `position_light_ground_red.json`、`position_light_ground_yellow.json`、`position_light_ground_proceed.json`、`position_light_pole_dark.json` 和 `position_light_pole_proceed.json`。机壳与灯圈有实际厚度；发光层从同一 JSON 的灯面提取。

落地式总宽和总高均为 12/16 方块，X=2～14，Y=0～12；灰色短杆连接底座与灯箱。背板高 10/16，Y=2～12，外壳材质使用 32×32 贴图。轮廓、灯位、灯面深度及 UV 均以手工源模型为准，包括小于 1/32 方块的细微调整。贴杆式保留完整的 16/16 高 MTR 柱段。所有调车信号的方块模型、发光层和碰撞边界均已统一转向 180°，修正放置后背面朝向玩家的问题。

资源生成：`python tools/generate_position_light_signals.py`。资源、灯位与碰撞边界检查：`python tools/check_position_light_signals.py`（预览需要 Pillow 和 NumPy），生成 `build/previews/position-light-preview.png`。信号计算每 10 tick 更新一次；闭塞、命名及绑定生命周期回归随 `gradlew build` 执行。预览由模型几何渲染，不代替 Minecraft 内实测。

灯圈和灯面外框直径为 3/16 方块，下方两灯中心等高，开放双白保持斜向排列。贴图保持 32×32，圆形灯面以透明贴图保留轮廓。

背板顶部顶点及斜边与两侧边的交接处采用圆弧过渡，再按 1/32 网格生成阶梯几何；底边两角保持直角，总宽、高和灯位保持不变。

`position_light_ground_dark.json` 是唯一手工几何源，生成器不会覆盖它，也不会覆盖现有贴图。编辑该文件后运行生成器，其他 31 个模型继承其几何和 UV，仅切换灯色、安装支撑及方向；碰撞边界同步重新计算。发光层直接读取生成后的灯面位置。

贴杆式信号柱使用插件内置的 `block/position_light/mtr_signal_pole.png`，原样复制自 MTR 的 `textures/block/metal.png`（16×16），不再引用外部 `mtr:block/metal`。来源与许可证见 `THIRD_PARTY_NOTICES.md`。

## 装饰性限速牌（PSR / AWI）

共十种方块：`permanent_speed_sign`、`permanent_speed_sign_double`、`advance_warning_sign`、`advance_warning_sign_double`，以及 `permanent_speed_arrow_left/both/right`、`advance_warning_arrow_left/both/right`。红色 PSR 使用圆牌，黄色 AWI 无论数字/数字还是车型/数字均使用同一三角背板；箭头使用小圆角矩形。它们只作装饰，不绑定主信号或节点，不改变列车限速、闭塞及进路。

- 圆牌直径 12/16 方块；圆形与圆角用平滑轮廓，文字和箭头不对齐像素网格。Alte DIN 1451 Mittelschrift Regular 字体以原始 TTF 和预制字形图集随包提供，采用 SIL OFL 1.1，箭头图案独立于字体。
- 红/黄描边向内加粗，不改变牌子外轮廓；箭头牌圆角半径为 0.027 方块。左、右单箭头分别取双向箭头对应一半的位置和尺寸，不把单箭头移到中央。
- 调试工具右键限速牌打开编辑界面，双行输入标签为“较低限速”和“较高限速”。单行及较高限速接受 1–999；双数字要求较低值小于较高值，绘制黑色分隔线。上栏可改为 1–4 位、字母开头的英文字母/数字车型短码（如 `HST`、`DMU`、`S7`），不绘制分隔线。
- AWI 上行与分隔线已向上调整以增加间距；PSR 车型文字向下调整，避免接触背板边缘。
- 限速牌支持置中/触底/触顶，箭头仅支持触底/触顶且调试工具右键立即切换。限速牌高 12/16 方块，箭头高 4/16 方块；通过平移安装位置在相邻方块边界贴合，不缩放牌子。
- 限速牌在上、箭头在下：限速牌触底，箭头触顶。箭头在上、限速牌在下：箭头触底，限速牌触顶。放置或修改安装方式时，相邻配对会同步朝向和互补安装位置；置中用于独立牌面，不调整相邻牌。
- 触顶的限速牌和箭头牌均使用 16/16 全高 MTR 杆；其余杆从方块底部延伸到牌后，在牌顶以内结束。轮廓选择范围包含牌面到杆后侧的完整厚度。
- 16 档旋转覆盖每个水平朝向及 22.5°、45°、67.5°；保留早期 `hanging` 状态兼容旧存档，新增 `centered` 区分置中。编辑内容、安装状态由服务端校验并保存。

资源生成与校验：

```powershell
python tools/generate_speed_signs.py
python tools/check_speed_signs.py
.\gradlew.bat build --no-daemon
```

生成器使用已提交的 `font/alte_din_1451_mittelschrift_regular.ttf`，不依赖本机字体来源目录；只更新限速牌资源，预览覆盖 `build/previews/speed_signs.png`。运行时外观由 `SpeedSignRenderer`、`SpeedSignMount` 和 `models/speed_sign_artwork.json` 共同决定，空的方块 JSON 仅提供粒子纹理，不是完整 Blockbench 预览。数字/字母图集、牌面和物品图标为预生成资源；运行游戏无需 Python。来源与许可说明见 `THIRD_PARTY_NOTICES.md`，JAR 内同时打包 `LICENSES/OFL-1.1.txt` 与该字体的来源说明。

## 9. 许可证与鸣谢

项目源代码采用 [MIT License](LICENSE)。Terminus Regular 字体采用 SIL Open Font License 1.1；版权和完整第三方许可见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。Minecraft、Forge 和 MTR 归各自权利人所有。

感谢 ChatGPT 5.6、ChatGPT 6 和 Codex 在架构梳理、代码实现、测试验证、资源整理与文档维护方面提供的协作支持。

# MTR_BRsignal_addon 迁移计划（分阶段）

> 状态：待确认执行。本文档只描述计划，不含代码改动。
> 基线提交：`e871b2a`（Snapshot: add server-authoritative block/request/authorization layer）。

## 架构护栏（不可违反原则）

> 以下为最高优先级，任何阶段、任何步骤都不得违反；若某步实现与护栏冲突，实现必须让位于护栏。

1. 只修改 `MTR_BRsignal_addon`，不得修改 MTR 本体源码或 jar；对 MTR 的影响仅通过 Mixin 在运行时覆盖。
2. 禁止大规模一次性重构；每步必须可编译、可回退。
3. 先补强服务端权威层，再逐步替换/删除旧逻辑；服务端权威未稳定前，不得删除仍有兜底作用的客户端逻辑。
4. `SectionState` 是服务端权威状态；客户端不得参与任何闭塞、安全、FCFS、Authorization 或停车判断。
5. 闭塞块由相邻 SignalFace 所绑定 Rail Node 切分；`occupied / reserved / locked` 按该闭塞块独立维护；车尾离开后独立释放。
6. **Section 释放 ≠ Request 释放**：Section 逐条释放，Request 只在整条进路走完后进入 `PASSED → RELEASED`。
7. `Request` = 一列车申请一条完整进路；由 Vehicle 根据完整 `immutablePath` 接近预告区域触发，不由 Signal 扫描全网车辆。
8. `triggerDistance = min(下一运营停车点距离, 前方约 4 个信号控制边界距离)` 只用于触发 Request；Request 是基于完整 `immutablePath` 提出的进路申请，但 Authorization 只是这条进路当前实际获批并锁闭的前缀，可随列车推进动态扩展/释放。
9. 自动 Dispatcher 使用 FCFS；人工 Dispatcher 可覆盖 FCFS 顺序，但不得绕过 SectionCheck、reserve/lock 或 MovementGate。
10. 没有有效 Authorization 时 Signal 默认红灯。
11. Movement Gate 是实际阻止车辆运行的执行层；Signal Aspect 是 Authorization 的表现，不是授权决策。
12. 拓扑失效必须撤销授权并停车，不得自动换路。

## 1. 目标架构不变式

1. `SectionState` 是服务端权威状态，客户端只显示。
2. 闭塞块 = 相邻 SignalFace 绑定节点之间的一组无向 Rail，不以单个 `rail.getHexId()` 为安全单位。
3. `Request` = 一列车基于完整 `immutablePath` 提出的一条完整进路申请；由 Vehicle 接近预告区域时触发，不由 Signal 扫描全网车辆。`Authorization` 是同一 Request 当前实际获批并锁闭的进路前缀；`Authorization ⊆ Request`，可为空（Request 转为 `DENIED`）。
4. 自动 Dispatcher 使用 FCFS；人工 Dispatcher 可覆盖 FCFS 顺序，但不能绕过 SectionCheck、reserve/lock 或 MovementGate。
5. 没有有效 Authorization 时 Signal 默认红灯。
6. Movement Gate 是实际阻止车辆运行的执行层。
7. Signal Aspect 是 Authorization 的表现，不是授权决策。
8. `occupied / reserved / locked` 是独立状态；Section 在车尾离开后独立释放。
9. **Section 释放 ≠ Request 释放**：Section 逐条释放，Request 只在整条进路走完后进入 `PASSED → RELEASED`。
10. `triggerDistance = min(下一运营停车点距离, 前方约 4 个信号控制边界距离)` 只用于触发 Request；Request 本身对应完整进路，Authorization 只覆盖当前可安全开放的前缀，不要求覆盖整条 Request。
11. 拓扑失效必须撤销授权并停车，不自动换路。

## 2. 约束与红线

- 只修改 `MTR_BRsignal_addon`，不改 MTR 本体源码或 jar；对 MTR 的影响仅通过 Mixin 在运行时覆盖。
- 禁止大规模一次性重构；每步可编译、可回退。
- 先补强服务端权威层，再逐步替换/删除旧逻辑。
- 客户端不得参与闭塞、安全、FCFS、Authorization、停车判断。

## 3. 当前状态摘要

当前是“旧客户端显示架构 + 新服务端权威架构”并存的过渡态：

- 服务端已有 `SectionStateManager`、`PathSnapshot`、`RouteRequest`、`SectionCheck`、`Dispatcher`、`Authorization`、`ServerAspectManager`、`MovementGate`、`RouteRequestManager`，并通过 `SimulatorMixin` / `VehicleMixin` / `DataMixin` 接入 MTR simulation 线程。
- 客户端显示已改为只读 `ServerAspectCache`，缺失回红。
- 但旧客户端闭塞引擎仍在 `SignalLogic` 中以死代码存在；`getActualAspect` 有两个 Mixin 竞争注入；占用状态存在两套计算；两个进路指示器显示条件不一致；MTR 原生 `railBlockedDistance` 被全局禁用而 MovementGate 尚无完整兜底。

## 4. 阶段总览（依赖顺序）

```text
阶段 0 基线
  ↓
阶段 1 服务端权威补强
  ↓
阶段 2 单一 Aspect 入口 + 删除旧客户端引擎
  ↓
阶段 3 Request / Authorization 语义对齐
  ↓
阶段 4 Movement Gate 安全接管
  ↓
阶段 5 显示层统一与去重
  ↓
阶段 6 人工调度（调度室运行，后置）
  ↓
阶段 7 文档与回归防护
```

阶段 1 完成后，阶段 2 才安全；阶段 3 与阶段 4 都依赖阶段 1/2；阶段 5 依赖阶段 1–4 的服务端事实；阶段 6 可在阶段 3/4 稳定后启动；阶段 7 全程可穿插。

## 5. 各阶段步骤

### 阶段 0：基线

#### Step 0.1 固化基线

- 目标：把当前混合状态提交/打 tag，作为可回退基线。
- 涉及文件：Git 仓库状态（已提交 `e871b2a`）。
- 影响旧逻辑：无逻辑影响。
- 验证方法：`git status` 干净，可回退到该提交。

### 阶段 1：服务端权威补强

#### Step 1.1 统一占用事实

- 目标：删除 `ServerAspectManager` 自行用 `VehicleSnapshot` 重算“信号区段占用”的路径，红灯判定改读 `SectionStateManager` 的权威 `occupied`。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/ServerAspectManager.java`
  - `src/main/java/org/mtrbr/server/SectionStateManager.java`
- 影响旧逻辑：`ServerAspectManager.isSectionOccupied(faces, face, vehicles)` 与 `RouteRequestManager.VehicleSnapshot` 的占用分支。
- 验证方法：`gradlew compileJava` 通过；运行日志 `[MTRBR-ASPECT]` / `[MTRBR-REQ]` 正常；测试“车头进入 Section → 对应 face 红，车尾离开 → 恢复”。

#### Step 1.2 服务端默认红全覆盖

- 目标：`ServerSignalAspectMixin` 在 `ServerAspectManager.get` 返回 null 时也返回 `RED`，不放行 MTR 原版 `getActualAspect`。
- 涉及文件：
  - `src/main/java/org/mtrbr/mixin/ServerSignalAspectMixin.java`
- 影响旧逻辑：未纳入拓扑/无作用节点的 MTR 信号机，此前可能走 MTR 原版灯色。
- 验证方法：放置一个未绑定节点的 MTR 信号，确认其服务端 aspect 为红；已管理信号不受影响。

#### Step 1.3 确认 Aspect 读取线程一致性

- 目标：确认 `ServerAspectManager.update` 只读取已发布快照，不直接读可变集合。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/ServerAspectManager.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
- 影响旧逻辑：跨线程读 `AUTHORIZATION_SNAPSHOTS` / `VEHICLE_SNAPSHOTS` 的可见性。
- 验证方法：单人集成服务器长时间运行无并发异常；Aspect 与 Request 日志一致。

### 阶段 2：单一 Aspect 入口 + 删除旧客户端引擎

#### Step 2.1 单一 Aspect Mixin 同时处理服务端与客户端

- 目标：`ServerSignalAspectMixin` 分支处理 `ServerLevel`（读 `ServerAspectManager`，缺失回红）与 `ClientLevel`（读 `ServerAspectCache`，缺失回红）。
- 涉及文件：
  - `src/main/java/org/mtrbr/mixin/ServerSignalAspectMixin.java`
  - `src/main/java/org/mtrbr/client/ServerAspectCache.java`
- 影响旧逻辑：客户端原先由 `BlockSignalBlockEntityMixin` 提供 aspect；迁移后仍从缓存提供，行为不变。
- 验证方法：客户端信号显示与迁移前一致；服务端信号正确。

#### Step 2.2 删除旧客户端 Mixin

- 目标：移除第二个 `getActualAspect` 注入，消除竞争。
- 涉及文件：
  - `src/main/java/org/mtrbr/mixin/BlockSignalBlockEntityMixin.java`
  - `src/main/resources/mtrbr.mixins.json`
- 影响旧逻辑：无（Step 2.1 已接管客户端显示）。
- 验证方法：`rg "BlockSignalBlockEntityMixin"` 无引用；客户端/服务端信号显示正常。

#### Step 2.3 删除 `SignalLogic` 旧闭塞链死代码

- 目标：删除旧闭塞链方法，只保留纯显示工具，并把 `getSignalAspect` 收敛为“只读 `ServerAspectCache`，缺失回红”。
- 涉及文件：
  - `src/main/java/org/mtrbr/logic/SignalLogic.java`
- 影响旧逻辑：删除旧客户端车辆检测、静态放绿、多车取最严、信号主动扫描；这些当前已是死代码。
- 验证方法：`rg "resolveAspect|getOccupied|isSectionOccupied|getOpenRouteBindings|findNextSignalsOnRoutes|isSignalOnAnyRoute"` 无剩余调用；编译通过；游戏内信号显示不变。

#### Step 2.4 清理 `SignalCache` 的闭塞解析用途

- 目标：`SignalCache` 只保留世界连线/HUD 的显示缓存，移除与旧闭塞链的耦合。
- 涉及文件：
  - `src/main/java/org/mtrbr/client/SignalCache.java`
  - `src/main/java/org/mtrbr/render/VehicleRouteReader.java`
  - `src/main/java/org/mtrbr/render/SignalOverlayRenderer.java`
- 影响旧逻辑：仅客户端显示扫描；服务端不受影响。
- 验证方法：手持工具时连线/标签/乘车 HUD 正常；`rg "SignalCache."` 确认调用点均为显示层。

### 阶段 3：Request / Authorization 语义对齐

#### Step 3.0 确认完整进路语义

- 目标：固定 Request 与 Authorization 的关系。
- 结论：`immutablePath` 是列车固有完整物理进路；`RouteRequest` 表示基于该完整进路提出的进路申请；`Authorization` 表示该 Request 当前实际获批并锁闭的进路前缀。
- `Authorization ⊆ Request`：Request 范围内无冲突时两者相等；范围内有冲突时 Authorization 只到冲突 Section 之前；第一段即冲突时 Authorization 为空，Request 转为 `DENIED`。
- 涉及文件：仅决策/文档，不改代码。
- 影响旧逻辑：决定 Step 3.1 方向。
- 验证方法：写入设计文档；运行时日志区分 Request 终点与当前 Authorization 终点。

#### Step 3.1 分离触发距离与授权进路长度

- 目标：`triggerDistance` 只决定何时创建 Request；Request 覆盖完整 `immutablePath`；Authorization 只覆盖 Request 内当前可安全开放的前缀，不要求覆盖整条 Request。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
  - `src/main/java/org/mtrbr/server/PathSnapshot.java`
- 影响旧逻辑：`findControlRange`、`maybeCreatePendingRequest`、部分授权截断、siding 出库窗口。
- 验证方法：单列车长进路 Request 终点等于完整进路终点；`[MTRBR-REQ]` 日志中 Authorization 终点不超过 Request 终点；出库车不提前锁死前方进路。

#### Step 3.2 保持 Section 释放 ≠ Request 释放

- 目标：Section 的 `occupied/reserved/locked` 在车尾离开该 Section 时独立释放；Authorization 随列车推进动态扩展并释放已驶过前缀；Request 只在完整进路走完后进入 `PASSED → RELEASED`。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/SectionStateManager.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
- 影响旧逻辑：`updateAuthorizedLifecycle`、`release`、`releaseAll`。
- 验证方法：长编组列车分段占用时，已驶离的 Section 释放而 Request 仍为 ACTIVE/PASSED；车尾越过终点后才 RELEASED。

#### Step 3.3 拓扑失效撤销授权并停车，不自动换路

- 目标：Path fingerprint 失效时撤销 Authorization、释放 reserved/locked、保留真实 occupied、MovementGate 停车，并重建 Request；默认不自动换路。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
  - `src/main/java/org/mtrbr/server/PathSnapshot.java`
- 影响旧逻辑：`matchesTopology`、`INVALID` 转移、`releaseAll`。
- 验证方法：运行时改变轨道拓扑，确认授权撤销、车辆停车、重新生成 Request；无自动换路；状态转移合法。

### 阶段 4：Movement Gate 安全接管

#### Step 4.1 收窄/评估 `railBlockedDistance` 禁用范围

- 目标：避免“全局禁用 MTR 原生阻塞 + MovementGate 边界 NaN”造成安全真空；明确哪些车辆走 addon gate，哪些仍需 MTR 原生兜底。
- 涉及文件：
  - `src/main/java/org/mtrbr/mixin/VehicleMixin.java`
  - `src/main/java/org/mtrbr/server/MovementGate.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
- 影响旧逻辑：`mtrbr$manualOverrideNativeBlock` 的 `return -1`；未受控/无边界车辆的停车。
- 验证方法：未进入受控区、前方无信号的车辆不再无约束冲过；已授权车辆正常过灯。

#### Step 4.2 停止边界兜底（消除 NaN 真空）

- 目标：`getStopBoundary` 在 `managed=false`、无 Request 或前方有信号时仍给出安全边界，不返回 NaN 导致不限速。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
- 影响旧逻辑：无 Request 时自由运行的分支。
- 验证方法：制造“无 Request 但前方有信号”场景，车辆在该信号前停止；`[MTRBR-GATE]` 日志显示 boundary 非 NaN。

#### Step 4.3 停车点 hook 稳健化/兜底

- 目标：降低 `VehicleMixin` 改写 MTR `simulateMoving` 局部变量（LVT index 5）的脆弱性，保留一个保守兜底。
- 涉及文件：
  - `src/main/java/org/mtrbr/mixin/VehicleMixin.java`
  - `src/main/java/org/mtrbr/server/MovementGate.java`
- 影响旧逻辑：停车点钳制与 tick 末 `enforce` 拉回。
- 验证方法：MTR 版本未变时行为一致；若未来 MTR 字节码变化，编译/运行能暴露问题。

### 阶段 5：显示层统一与去重

#### Step 5.1 两个进路指示器统一显示条件

- 目标：色灯式改为与 LED 一致，读取服务端 `authorizationId` / `routeContent`，不再用“非红灯 + 有 route 绑定”。
- 涉及文件：
  - `src/main/java/org/mtrbr/render/ColorLightIndicatorRenderer.java`
  - `src/main/java/org/mtrbr/render/LedIndicatorRenderer.java`
- 影响旧逻辑：色灯式旧的显示判定。
- 验证方法：同一授权状态下 LED 与色灯显示一致；未授权时两者都熄灭。

#### Step 5.2 收敛 `findAppliedNode` 与客户端信号拓扑来源

- 目标：客户端不再自行复刻节点搜索；显示层使用服务端下发的 `SignalFace` / node binding 镜像。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/SignalTopology.java`
  - `src/main/java/org/mtrbr/logic/SignalLogic.java`
  - 显示渲染器
- 影响旧逻辑：客户端/服务端节点搜索结果漂移。
- 验证方法：同一信号机在客户端显示与服务端拓扑中的作用节点一致；连线/标签位置一致。

### 阶段 6：人工调度（调度室运行，后置）

> 本阶段先搁置实现，但保留为规划。目标是让人工调度尽量接近“仿真调度室”的运行方式：集中监控、人工选择、审计留痕，且所有操作仍走服务端安全链路。

#### Step 6.1 完善服务端调度 API

- 目标：以现有 `/mtrbr manual_override`、`/mtrbr priority`、`/mtrbr revoke_pending` 为底座，补齐“列出 WAITING Request、选择/批准、撤销、审计”的服务端接口。
- 涉及文件：
  - `src/main/java/org/mtrbr/command/MTRBRCommands.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
  - `src/main/java/org/mtrbr/server/Dispatcher.java`
- 影响旧逻辑：人工优先级只作用于 `request`，暂不作用于 `pendingRequest`；撤销只撤未进入的 Authorization。
- 验证方法：命令操作不改 SectionCheck 结果；非法操作被拒绝；审计日志记录每次人工操作。

#### Step 6.2 调度室视图（面板/GUI）

- 目标：提供接近真实调度室的集中视图：实时显示 Section 占用/预留/锁闭、Request 状态、Authorization、车辆位置；支持从 WAITING 列表人工选择（覆盖 FCFS 顺序）。
- 涉及文件：
  - 新增 screen/面板，复用服务端快照 API
- 影响旧逻辑：不引入新的安全决策；只展示和发送调度操作。
- 验证方法：面板数据与服务端快照一致；人工选择后仍走 SectionCheck/reserve/lock。

#### Step 6.3 调度台方块 + 调度工具（可选实体化）

- 目标：如把调度室落地为实体，新增一个调度台方块和配套工具；工具用于打开调度室视图。
- 涉及文件：
  - 新增 block/item 注册、方块实体、渲染/屏幕
- 影响旧逻辑：无安全影响，仅操作入口。
- 验证方法：放置调度台、使用工具打开面板；操作只改调度选择，不绕过安全检查。

#### Step 6.4 审计与异常恢复

- 目标：记录 Dispatcher Override、优先级、撤销等操作；异常占用与未授权进入可追溯。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`（现有 `audit` 列表）
- 影响旧逻辑：审计字段扩展。
- 验证方法：查询审计日志，核对人工操作与 SectionState/Authorization 变化一致。

### 阶段 7：文档与回归防护

#### Step 7.1 同步设计文档与实现

- 目标：更新 `设计大纲.md` / `railway_block_design.md`，消除“完整进路 vs 分段”等文档矛盾。
- 涉及文件：
  - `设计大纲.md`
  - `railway_block_design.md`
- 影响旧逻辑：无运行时影响。
- 验证方法：文档与代码/日志一致。

#### Step 7.2 建立静态回归检查

- 目标：用脚本固定检查禁止模式：客户端 `MinecraftClientData.vehicles` 遍历、两个 aspect mixin、`findAppliedNode` 双实现、`railBlockedDistance` 无兜底禁用等。
- 涉及文件：新增检查脚本（建议放 `tools/`）。
- 影响旧逻辑：防止后续修改把旧路径重新接回。
- 验证方法：本地/CI 跑检查脚本零命中。

## 6. 新增方块/物品清单

### 核心迁移阶段

- 新增方块：0 个。
- 新增物品：0 个。
- 复用 MTR 原生信号机 + addon 已有的两个指示器方块：
  - `led_indicator`（LED 式进路指示器）
  - `indicator_1`（色灯式进路指示器）

### 人工调度阶段（后置，可选实体化）

- 可选新增方块：1 个 `dispatcher_console`（调度台）：集中查看 SectionState/Request/Authorization，人工选择 WAITING Request、设置优先级、撤销未进入授权。
- 可选新增物品：1 个 `dispatcher_tool`（调度工具）：用于打开调度室视图。

## 7. 验证总览

每个阶段至少执行：

1. `gradlew compileJava`（或 `gradlew build`）通过。
2. 静态检查 `rg` 确认旧符号无剩余活动调用。
3. 游戏内场景验证：
   - 单列车进路申请与信号显示。
   - 两列车 FCFS 与冲突占用。
   - 车头进入/车尾离开的 Section 释放。
   - 拓扑变化 → 授权撤销、停车、重建 Request、不自动换路。
   - 人工调度（阶段 6）不绕过 SectionCheck。

## 8. 二次审计修订（待执行）

> 2026-08-20 二次审计后确定。本节是护栏的补充说明，不推翻护栏；若与前文旧描述冲突，以本节为准。

### 8.1 两种“区段”概念并存

- 底层 `Section`：一条无向 Rail，`SectionState` 目前按它记录 `occupied / reserved / locked`，是状态与释放的最小粒度。
- `闭塞区间`（Signal Block）：相邻 SignalFace 边界之间的 Rail 集合，是“同一时间只允许一辆车”的互斥、授权与停车原子单位。
- 两者都要用：Section 负责状态与逐段释放；闭塞区间负责互斥、Authorization 前缀与 MovementGate 停车。
- 当前“一个闭塞区间进两辆车”的 bug，来自互斥/停车仍按单条 Rail 判断，而不是按闭塞区间判断。

### 8.2 R1 修复“一个闭塞区间进两辆车”

- 目标：闭塞区间级互斥；授权与物理停车边界都以闭塞区间为准，而非仅按单条 Rail。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
  - `src/main/java/org/mtrbr/server/SectionStateManager.java`
  - `src/main/java/org/mtrbr/server/ServerAspectManager.java`
  - `src/main/java/org/mtrbr/server/PathSnapshot.java`
- 影响旧逻辑：当前碰撞边界按单条 Rail 判断，两车在不同 Rail 但同属一个闭塞区间时可能同时进入。
- 验证方法：构造两车同时接近同一闭塞区间的场景，第二辆车在该区间外停下；仅一辆车获得授权并进入。

### 8.3 R2 SectionCheck 重新成为唯一安全检查入口；删除 getMovementBoundary

- 目标：让 `clearancePrefix` 的逐段可用性检查回到 `SectionCheck`（保留详细结果），并删除已无调用者的 `getMovementBoundary`。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/SectionCheck.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
- 影响旧逻辑：当前 `clearancePrefix` 内联检查；`getMovementBoundary` 为死代码。
- 验证方法：人工 `approve` 仍走 SectionCheck / reserve / lock；`rg "getMovementBoundary"` 无引用。

### 8.4 R3 信号机↔闭塞区间/Section 绑定持久化到存档

- 目标：信号机 face → 保护 Rail Section 集合的映射严格写入存档数据；服务端拓扑从存档加载，不再从车辆 path 动态推导。
- 涉及文件：
  - `src/main/java/org/mtrbr/data/RouteBindingsSavedData.java`（或新增 SavedData）
  - `src/main/java/org/mtrbr/server/SignalTopology.java`
  - `src/main/java/org/mtrbr/server/ServerAspectManager.java`
  - `src/main/java/org/mtrbr/server/ServerSignalRegistry.java`
- 影响旧逻辑：`ServerAspectManager.isSectionOccupied` 目前靠 `VehicleSnapshot` 推导 face→section 映射。
- 验证方法：世界重载后绑定仍存在；无车辆快照时占用信号仍正确变红；存档 NBT 可检查绑定。

### 8.5 R4 周期任务 1s 化

- 目标：SectionState 快照发布与 Aspect/Dispatcher 同步降到约每秒一次（20 tick），避免每 tick 全量计算。
- 涉及文件：
  - `src/main/java/org/mtrbr/MTRBR.java`
  - `src/main/java/org/mtrbr/server/ServerAspectManager.java`
  - `src/main/java/org/mtrbr/server/SectionStateManager.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
- 影响旧逻辑：当前 `ServerAspectManager.update` 每 server tick 执行，SectionState 每 simulation tick 发布全量快照。
- 验证方法：日志节奏约 1s；信号/调度在状态变化时仍及时更新（变化触发 + 1s 兜底）。

## 9. 三次审查后的修订与调度面板待办

> 2026-08-21 三次审查后确定。优先级：9.1 最高；其余按顺序执行。

### 9.1 Request 完整进路与 Authorization 前瞻窗口分离（修正 1.1）

- 结论：`Request` 终点 = 完整 `immutablePath` 终点；`Authorization` / `Aspect` 只开放到当前车头前方 `min(下一运营停车点, 约 4 个信号边界)` 的预告窗口，随列车推进动态向前扩展。
- 根因：若 Authorization 扩展到整条空闲进路，`resolveAspect` 会把整条进路全部投影为绿灯；之前用“缩短 Request”压绿灯是错误耦合。
- 修复方向：
  - `findControlRange` 保留 `requestEndDistance`（完整路径）与 `lookaheadEnd`（min 窗口）两个量。
  - Request 的 Section / SignalFace 序列使用 `requestEndDistance`。
  - `clearancePrefix` / `extendAuthorization` 以 `lookaheadEnd` 为授权扩展上限。
  - `resolveAspect` 维持现状，红点自然落在授权前缀之外，绿灯链只覆盖近前几个信号。
- 涉及文件：
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
  - `src/main/java/org/mtrbr/server/PathSnapshot.java`
- 验证方法：单列车长进路时，仅近前约 4 个信号 / 到下一停站范围内出现绿灯，远端信号保持黄 / 双黄 / 红；Request 终点仍等于完整路径终点。

### 9.2 调度面板安全与兼容

- 9.2.1 权限校验：`DispatcherActionPacket` 的 approve / revoke 必须校验玩家权限（对齐 `/mtrbr` 的 OP 门槛），普通玩家不得人工调度。
- 9.2.2 协议版本升级：新增调度包与 `SyncSignalAspectsPacket` 格式变更后，`Network.PROTOCOL_VERSION` 需升级（如 `5`）。
- 涉及文件：
  - `src/main/java/org/mtrbr/network/DispatcherActionPacket.java`
  - `src/main/java/org/mtrbr/network/Network.java`

### 9.3 SectionState 快照 1s 节流（补全 R4）

- `MTRBR.onServerTick` 已 1s；但 `SectionStateManager.endTick` 仍每 simulation tick 发布全量快照。
- 改为“状态变化触发 + 1s 兜底”，避免每 tick 全量 publish。
- 涉及文件：`src/main/java/org/mtrbr/server/SectionStateManager.java`。

### 9.4 调度面板列表与显示修复

- 9.4.1 列表无法滚动：检查 `DispatcherList` 高度、滚轮事件捕获与滚动条位置。
- 9.4.2 缺失车辆 ID：当前 `RequestSnapshot` 只输出 `request != null` 的车辆，无 Request 的车辆被漏掉；按需改为输出所有受管 / 在场车辆。
- 9.4.3 `route` / `dest` 显示严重不准：重做 `getThisRouteName/Number/Destination/StationName` 到列字段的映射与空值处理。
- 9.4.4 刷新条件：当前仅在条目数变化时刷新，改为内容变化即刷新。
- 涉及文件：
  - `src/main/java/org/mtrbr/screen/DispatcherScreen.java`
  - `src/main/java/org/mtrbr/client/ClientDispatcherData.java`
  - `src/main/java/org/mtrbr/server/RouteRequestManager.java`
  - `src/main/java/org/mtrbr/network/SyncDispatcherDataPacket.java`

### 9.5 调度面板未来增强（后置）

- 增加搜索框。
- 支持按列排序。
- 重做 UI 背景与整体美观。

### 9.6 `SignalBlockSavedData.rebuild` 岔区验证

- 在岔区 / 多节点 / 同一节点被多个 face 绑定的场景验证贪心走查是否正确，必要时改为更严格的拓扑切分。

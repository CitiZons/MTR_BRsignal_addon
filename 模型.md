# MTR_BRsignal_addon 模型与贴图清单（现状 2026-08-24）

> 本文档对应**当前代码实际使用**的资源约定；改模型/贴图前先读这里。

## 0. 当前渲染架构总览

有两个进路指示器方块，渲染方式不同：

| 方块 | 内容类型 | 模型来源 | 渲染方式 |
|---|---|---|---|
| `indicator_1`（色灯式） | `route=1/2/4/5` | null 外壳模型 + `indicator_*_<route>.json` | route 模型只提供灯块边界；`ColorLightIndicatorRenderer` 用白色 `QueuedRenderLayer.LIGHT` 绘制每个灯块六面 |
| `led_indicator`（LED 式） | `path=0..20 / A..Z / UF,US,DF,DS` | `led_indicator.bbmodel` → 构建期生成 `led_indicator.json`（含 22.5/45/67.5 旋转变体） | JSON 方块模型 + `LedIndicatorRenderer` 用贴图四边形画屏幕内容 |

- 色灯式 route 模型首次解析后会写入 `config/mtr_brsignal_addon/route-light-cache.json`；修改 route 模型后需显式删除该缓存文件再启动游戏。
- LED 式改 `.bbmodel` 后需重新生成 `led_indicator*.json`。
- 两个工具（信号机调试工具 / 进路工具）暂用默认缺失贴图，带 Shift 悬停用法提示。

## 1. 贴图清单

统一在 `assets/mtr_brsignal_addon/textures/block/`。注意：bbmodel 的 UV 空间是 **32×32**（`uv_width/uv_height`），
与实际 PNG 尺寸无关；渲染器按 32 空间归一化。

| 文件 | 尺寸 | 用途 | 备注 |
|---|---|---|---|
| `indicator_1.png` | 64×64 | 色灯式**正面**面板（黑背板 + 斜条） | 透明背景已补成不透明黑，斜条提亮到 255 |
| `indicator_1_back.png` | 64×64 | 色灯式**背面**面板 | 保持暗色斜条 + 透明底，不补黑不提亮 |
| `white.png` | 16×16 | 色灯式 route 灯块 | 以 `QueuedRenderLayer.LIGHT` 全亮绘制，不受环境光照影响 |
| `grey.png` / `black.png` | 16×16 | 机壳/立柱/无贴图面填充 | bbmodel 内未贴图的面用 black 补 |
| `white.png` | 16×16 | 工具/文字框等通用 | |
| `path/path_0.png` … `path/path_20.png` | 12×12 | LED 数字显示 | 正立、大写 |
| `path/path_A.png` … `path/path_Z.png` | 12×12 | LED 字母显示 | |
| `path/path_UF.png` `path_US.png` `path_DF.png` `path_DS.png` `path_UP.png` `path_DN.png` `path_UR.png` `path_DR.png` | 12×12 | LED 双大写组合 | |
| `path/path_adl.png` `path_adr.png` `path_arl.png` `path_arr.png` `path_atl.png` `path_atm.png` `path_atr.png` | 12×12 | LED 小写箭头组合 | |

## 2. 模型结构

### 色灯式 `indicator_1_NULL.bbmodel`（92 元素，x −16..16 → 2×2 面板）
- 面板元素：南面 = `indicator_1`（正面），北面 = `indicator_1_back`（背面）；面板 4 侧边透明。
- route 元素（texture 2）：动态图层，运行时按进路状态显隐。
- 机壳/立柱：grey/black，未贴图面用 black 补。
- 全部几何由 `ColorLightModel` 在运行时解析（逐面 UV、元素旋转支持）；blockstate 指向空模型。

### LED 式 `led_indicator.bbmodel`（15 元素）
- 屏幕面：黑色元素北面位于 z=5/16，x 5..11，y 1..7。
- 构建期转成 `led_indicator.json`（UV 按 32→16 缩放）+ `_22_5/_45/_67_5` 旋转变体。

### 旋转
- 两个方块都有 `is_22_5` / `is_45` 属性（放置时按玩家朝向自动设置），blockstate 16 变体。
- 色灯式渲染器按 `-(angle)`、LED 按 `-(angle+180)` 旋转显示层。

## 3. 进路内容 ↔ 贴图映射

- `route=X`：X ∈ {1,2,4,5}；对应 route 模型中的命名灯块。方块静态模型保持 null 外壳。
- `path=Y`：Y ∈ {0..20，1-4 个大写字母，UF/US/DF/DS/UP/DN/UR/DR，以及 adl/adr/arl/arr/atl/atm/atr}；映射 `textures/block/path/path_Y.png`。
- `path=NULL` / `route=NULL`：不显示。
- 显示条件：绑定信号机**非红灯**且有有效绑定；红灯立即熄灭。

## 4. 改资源的流程

1. 色灯式外壳：改 null 模型；route 灯块：改对应 `indicator_*_<route>.json`，再删除 `config/mtr_brsignal_addon/route-light-cache.json`。
2. LED 式：改 `led_indicator.bbmodel` → 重新生成 JSON（可复用 `tools/` 里的脚本思路）。
3. 贴图：直接替换 PNG；注意 12×12 的 path 贴图保持正立、透明背景按需处理。

## 5. 工具脚本

- `tools/bbmodel_to_obj.py`：色灯式 bbmodel → Forge OBJ（保留备查；当前主渲染已改运行时解析）。
- `tools/inspect_nbt.py`：检查存档 NBT/方块实体绑定。
